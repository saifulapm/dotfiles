{
  "//": "Generated by bin/theme-apply — the theme-dependent half of the vicinae config. Listed after overrides.json in VICINAE_OVERRIDES, so these keys win.",
  "font": { "normal": { "family": "{{font.mono}}" } },
  "launcher_window": { "rounding": {{shape.radius}} }
}
