# generated by theme-apply — do not edit
# Consumed by the oma apps (omacalc/omawrite/omacut), whose builds are patched
# by run_after_13-oma-apps.sh to read ~/.local/state/qshell/oma-theme.toml
# instead of omarchy's ~/.local/state/omarchy/current/theme/colors.toml.
# Their parser is flat key = "value" with exactly these five keys; mode may be
# omitted (they infer dark/light from background luminance) but is emitted
# since meta.mode always resolves.
mode = "{{meta.mode}}"
background = "{{surface.1}}"
foreground = "{{text.primary}}"
accent = "{{accent.accent}}"
selection = "{{surface.3}}"
