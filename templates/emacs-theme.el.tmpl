;; generated by theme-apply — do not edit.  -*- lexical-binding: t; -*-
;;
;; The named-color layer for the qshell-dark / qshell-light Emacs themes
;; (saifulapm/emacs.d, themes/qshell-{dark,light}-theme.el). Those themes are
;; Modus derivatives: modus-themes keeps every semantic mapping and face, and
;; this file swaps the root colors the mappings resolve to. The list mirrors
;; the named (hex-valued) entries of modus-themes-vivendi-palette — the same
;; set as operandi's, which is why one rendered file serves both polarities.
;; Derived shades (warmer/cooler/faint, bg tints) are precomputed by
;; bin/theme-apply's emacs block; see the mapping rationale there.  Every
;; foreground below comes through that block's contrast floor, so the hue
;; entries carry a derived value rather than the raw ansi color they are
;; blended from — the sole exceptions are the terminal endpoints at the
;; bottom, deliberately left raw so they match the terminal exactly.
(setq qshell-theme-palette
      '(;; base surfaces and foregrounds
        (bg-main "{{surface.0}}")
        (bg-dim "{{surface.1}}")
        (fg-main "{{text.primary}}")
        (fg-dim "{{emacs.fg-dim}}")
        (fg-alt "{{emacs.fg-alt}}")
        (bg-active "{{surface.3}}")
        (bg-inactive "{{surface.2}}")
        (border "{{emacs.border}}")
        ;; hues: base, warmer, cooler, faint, intense
        (red "{{emacs.red}}")
        (red-warmer "{{emacs.red-warmer}}")
        (red-cooler "{{emacs.red-cooler}}")
        (red-faint "{{emacs.red-faint}}")
        (red-intense "{{emacs.red-intense}}")
        (green "{{emacs.green}}")
        (green-warmer "{{emacs.green-warmer}}")
        (green-cooler "{{emacs.green-cooler}}")
        (green-faint "{{emacs.green-faint}}")
        (green-intense "{{emacs.green-intense}}")
        (yellow "{{emacs.yellow}}")
        (yellow-warmer "{{emacs.yellow-warmer}}")
        (yellow-cooler "{{emacs.yellow-cooler}}")
        (yellow-faint "{{emacs.yellow-faint}}")
        (yellow-intense "{{emacs.yellow-intense}}")
        (blue "{{emacs.blue}}")
        (blue-warmer "{{emacs.blue-warmer}}")
        (blue-cooler "{{emacs.blue-cooler}}")
        (blue-faint "{{emacs.blue-faint}}")
        (blue-intense "{{emacs.blue-intense}}")
        (magenta "{{emacs.magenta}}")
        (magenta-warmer "{{emacs.magenta-warmer}}")
        (magenta-cooler "{{emacs.magenta-cooler}}")
        (magenta-faint "{{emacs.magenta-faint}}")
        (magenta-intense "{{emacs.magenta-intense}}")
        (cyan "{{emacs.cyan}}")
        (cyan-warmer "{{emacs.cyan-warmer}}")
        (cyan-cooler "{{emacs.cyan-cooler}}")
        (cyan-faint "{{emacs.cyan-faint}}")
        (cyan-intense "{{emacs.cyan-intense}}")
        ;; off-palette accent hues
        (rust "{{emacs.rust}}")
        (gold "{{emacs.gold}}")
        (olive "{{emacs.olive}}")
        (slate "{{emacs.slate}}")
        (indigo "{{emacs.indigo}}")
        (maroon "{{emacs.maroon}}")
        (pink "{{emacs.pink}}")
        ;; hue-tinted backgrounds
        (bg-red-intense "{{emacs.bg-red-intense}}")
        (bg-green-intense "{{emacs.bg-green-intense}}")
        (bg-yellow-intense "{{emacs.bg-yellow-intense}}")
        (bg-blue-intense "{{emacs.bg-blue-intense}}")
        (bg-magenta-intense "{{emacs.bg-magenta-intense}}")
        (bg-cyan-intense "{{emacs.bg-cyan-intense}}")
        (bg-red-subtle "{{emacs.bg-red-subtle}}")
        (bg-green-subtle "{{emacs.bg-green-subtle}}")
        (bg-yellow-subtle "{{emacs.bg-yellow-subtle}}")
        (bg-blue-subtle "{{emacs.bg-blue-subtle}}")
        (bg-magenta-subtle "{{emacs.bg-magenta-subtle}}")
        (bg-cyan-subtle "{{emacs.bg-cyan-subtle}}")
        (bg-red-nuanced "{{emacs.bg-red-nuanced}}")
        (bg-green-nuanced "{{emacs.bg-green-nuanced}}")
        (bg-yellow-nuanced "{{emacs.bg-yellow-nuanced}}")
        (bg-blue-nuanced "{{emacs.bg-blue-nuanced}}")
        (bg-magenta-nuanced "{{emacs.bg-magenta-nuanced}}")
        (bg-cyan-nuanced "{{emacs.bg-cyan-nuanced}}")
        ;; tinted bg/fg companion pairs
        (bg-clay "{{emacs.bg-clay}}")
        (fg-clay "{{emacs.fg-clay}}")
        (bg-ochre "{{emacs.bg-ochre}}")
        (fg-ochre "{{emacs.fg-ochre}}")
        (bg-lavender "{{emacs.bg-lavender}}")
        (fg-lavender "{{emacs.fg-lavender}}")
        (bg-sage "{{emacs.bg-sage}}")
        (fg-sage "{{emacs.fg-sage}}")
        ;; graphs
        (bg-graph-red-0 "{{ansi.red}}")
        (bg-graph-red-1 "{{emacs.bg-graph-red-1}}")
        (bg-graph-green-0 "{{ansi.green}}")
        (bg-graph-green-1 "{{emacs.bg-graph-green-1}}")
        (bg-graph-yellow-0 "{{ansi.yellow}}")
        (bg-graph-yellow-1 "{{emacs.bg-graph-yellow-1}}")
        (bg-graph-blue-0 "{{ansi.blue}}")
        (bg-graph-blue-1 "{{emacs.bg-graph-blue-1}}")
        (bg-graph-magenta-0 "{{ansi.magenta}}")
        (bg-graph-magenta-1 "{{emacs.bg-graph-magenta-1}}")
        (bg-graph-cyan-0 "{{ansi.cyan}}")
        (bg-graph-cyan-1 "{{emacs.bg-graph-cyan-1}}")
        ;; interactive UI backgrounds
        (bg-completion "{{emacs.bg-completion}}")
        (bg-popup "{{surface.1}}")
        (bg-hover "{{emacs.bg-hover}}")
        (bg-hover-secondary "{{emacs.bg-hover-secondary}}")
        (bg-hl-line "{{emacs.bg-hl-line}}")
        (bg-region "{{surface.3}}")
        ;; mode line
        (bg-mode-line-active "{{surface.3}}")
        (border-mode-line-active "{{emacs.border-mode-line}}")
        (bg-mode-line-inactive "{{surface.1}}")
        (fg-mode-line-inactive "{{emacs.fg-mode-line-inactive}}")
        (border-mode-line-inactive "{{surface.2}}")
        (modeline-err "{{emacs.modeline-err}}")
        (modeline-warning "{{emacs.modeline-warning}}")
        (modeline-info "{{emacs.modeline-info}}")
        ;; tab bar
        (bg-tab-bar "{{surface.1}}")
        (bg-tab-current "{{surface.0}}")
        (bg-tab-other "{{surface.2}}")
        ;; diffs
        (bg-added "{{emacs.bg-added}}")
        (bg-added-faint "{{emacs.bg-added-faint}}")
        (bg-added-refine "{{emacs.bg-added-refine}}")
        (bg-added-fringe "{{emacs.bg-added-fringe}}")
        (fg-added "{{emacs.fg-added}}")
        (fg-added-intense "{{emacs.fg-added-intense}}")
        (bg-changed "{{emacs.bg-changed}}")
        (bg-changed-faint "{{emacs.bg-changed-faint}}")
        (bg-changed-refine "{{emacs.bg-changed-refine}}")
        (bg-changed-fringe "{{emacs.bg-changed-fringe}}")
        (fg-changed "{{emacs.fg-changed}}")
        (fg-changed-intense "{{emacs.fg-changed-intense}}")
        (bg-removed "{{emacs.bg-removed}}")
        (bg-removed-faint "{{emacs.bg-removed-faint}}")
        (bg-removed-refine "{{emacs.bg-removed-refine}}")
        (bg-removed-fringe "{{emacs.bg-removed-fringe}}")
        (fg-removed "{{emacs.fg-removed}}")
        (fg-removed-intense "{{emacs.fg-removed-intense}}")
        (bg-diff-context "{{surface.1}}")
        ;; parens
        (bg-paren-match "{{emacs.bg-paren-match}}")
        (bg-paren-expression "{{emacs.bg-paren-expression}}")
        ;; Terminal endpoints — pinned to the RAW ansi tokens, the one place in
        ;; this file the contrast floor is deliberately not applied.  Modus maps
        ;; fg-term-red onto `red' and fg-term-red-bright onto `red-warmer', so
        ;; without these literals a ghostel/eshell/compile buffer would inherit
        ;; the floored syntax colors and stop matching what foot renders for the
        ;; same escape sequence.  Terminal output should be byte-faithful to the
        ;; theme; only Emacs's own syntax needs the legibility lift.
        (bg-term-black "{{ansi.black}}")
        (fg-term-black "{{ansi.black}}")
        (bg-term-black-bright "{{ansi.bright-black}}")
        (fg-term-black-bright "{{ansi.bright-black}}")
        (bg-term-red "{{ansi.red}}")
        (fg-term-red "{{ansi.red}}")
        (bg-term-red-bright "{{ansi.bright-red}}")
        (fg-term-red-bright "{{ansi.bright-red}}")
        (bg-term-green "{{ansi.green}}")
        (fg-term-green "{{ansi.green}}")
        (bg-term-green-bright "{{ansi.bright-green}}")
        (fg-term-green-bright "{{ansi.bright-green}}")
        (bg-term-yellow "{{ansi.yellow}}")
        (fg-term-yellow "{{ansi.yellow}}")
        (bg-term-yellow-bright "{{ansi.bright-yellow}}")
        (fg-term-yellow-bright "{{ansi.bright-yellow}}")
        (bg-term-blue "{{ansi.blue}}")
        (fg-term-blue "{{ansi.blue}}")
        (bg-term-blue-bright "{{ansi.bright-blue}}")
        (fg-term-blue-bright "{{ansi.bright-blue}}")
        (bg-term-magenta "{{ansi.magenta}}")
        (fg-term-magenta "{{ansi.magenta}}")
        (bg-term-magenta-bright "{{ansi.bright-magenta}}")
        (fg-term-magenta-bright "{{ansi.bright-magenta}}")
        (bg-term-cyan "{{ansi.cyan}}")
        (fg-term-cyan "{{ansi.cyan}}")
        (bg-term-cyan-bright "{{ansi.bright-cyan}}")
        (fg-term-cyan-bright "{{ansi.bright-cyan}}")
        (bg-term-white "{{ansi.white}}")
        (fg-term-white "{{ansi.white}}")
        (bg-term-white-bright "{{ansi.bright-white}}")
        (fg-term-white-bright "{{ansi.bright-white}}")))
