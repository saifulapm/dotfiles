# Herd-like on-demand PostgreSQL (managed by chezmoi) — see
# mysql84-proxy.socket for the pattern.
[Unit]
Description=PostgreSQL 18 on-demand socket (127.0.0.1:5432)

[Socket]
ListenStream=127.0.0.1:5432

[Install]
WantedBy=sockets.target
