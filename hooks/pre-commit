#!/bin/sh
# Auto-format staged QML with qmlformat, then re-stage.
set -e
files=$(git diff --cached --name-only --diff-filter=ACM | grep '\.qml$' || true)
[ -z "$files" ] && exit 0
for f in $files; do
  /usr/lib64/qt6/bin/qmlformat -i "$f"
  git add "$f"
done
