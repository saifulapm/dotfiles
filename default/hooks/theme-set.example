#!/bin/bash
# Example hook: Called after theme is changed
# Copy this to ~/.config/nova/hooks/theme-set and make executable
#
# Arguments:
#   $1 - Theme name that was applied

THEME="$1"

echo "Theme hook: $THEME applied"

# Example: Sync Obsidian theme
# if [[ -d "$HOME/Documents/Obsidian" ]]; then
#     cp ~/.config/nova/current/theme/obsidian.css "$HOME/Documents/Obsidian/.obsidian/themes/nova.css"
# fi

# Example: Restart specific apps
# pkill -x ghostty && setsid ghostty &
