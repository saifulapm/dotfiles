#!/bin/bash
# Hook: font-set
# Called after font is changed
# Arguments: $1 = font name

FONT="$1"
echo "Font changed to: $FONT"

# Example: Restart specific apps that cache fonts
# pkill -USR1 alacritty 2>/dev/null || true
