# machine-specific, auto-populated by quickshell at startup
shell/.qmlls.ini
bench/.last-qs.log

# per-directory agent memory, auto-populated by claude code reviews
**/.claude/

# python bytecode, from importing bin/otp-import as a module while testing
bin/__pycache__/
